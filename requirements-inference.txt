# ============================================================
# LLM Fine-Tuning Platform - Inference-only requirements
# (FastAPI service + CLI inference + metrics; no training deps)
# Verified on Python 3.14 CPU.
# ============================================================

# Core ML (CPU-capable)
torch>=2.4,<3

# HuggingFace
transformers>=4.46,<6
tokenizers>=0.20
accelerate>=1.0
peft>=0.13,<1

# Evaluation (used by POST /evaluate and scripts/evaluate.py --from-predictions)
rouge-score>=0.1.2
nltk>=3.9
evaluate>=0.4.2

# Config / data
omegaconf>=2.3
pandas>=2.2
numpy>=1.26

# Utilities
loguru>=0.7
pyyaml>=6.0
rich>=13.7

# API
fastapi>=0.112
uvicorn[standard]>=0.30
pydantic>=2.8
httpx>=0.27
